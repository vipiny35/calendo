{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for the calendar popover and the settings window.",
  "windows": ["calendar", "settings"],
  "permissions": ["core:default"]
}
