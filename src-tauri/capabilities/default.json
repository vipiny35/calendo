{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for the calendar popover, the events popover, and the settings window.",
  "windows": [
    "calendar",
    "events",
    "settings"
  ],
  "permissions": [
    "core:default"
  ]
}
